rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Mengizinkan siapa saja membaca file di Storage
    match /{allPaths=**} {
      allow read: if true;
      
      // Mengizinkan upload jika user login
      allow write: if request.auth != null;
    }
  }
}
